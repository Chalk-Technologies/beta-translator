# File generated by github.com/posener/goaction. DO NOT EDIT.

name: main
inputs:
  export:
    default: false
    description: "export all translations from the notion DB"
    required: false
  importFile:
    description: "import all translations file"
    required: false
  importField:
    default: Text English
    description: "import all translations into field"
    required: false
  notionSecret:
    description: "notion secret"
    required: false
  db:
    description: "notion database id"
    required: true
  uploadRepo:
    default: Chalk-Technologies/beta-web
    description: "The repo to upload to, including repo owner and without the .git extension, not case-sensitive"
    required: false
  uploadPath:
    default: public/translations/
    description: "The repo to upload to, including repo owner and without the .git extension, not case-sensitive"
    required: false
runs:
  using: docker
  image: Dockerfile
  args:
  - "-export=${{ inputs.export }}"
  - "-importFile=${{ inputs.importFile }}"
  - "-importField=${{ inputs.importField }}"
  - "-notionSecret=${{ inputs.notionSecret }}"
  - "-db=${{ inputs.db }}"
  - "-uploadRepo=${{ inputs.uploadRepo }}"
  - "-uploadPath=${{ inputs.uploadPath }}"
