# File generated by github.com/posener/goaction. DO NOT EDIT.

name: main
inputs:
  export:
    default: false
    description: "export all translations from the notion DB"
    required: false
  upload:
    default: false
    description: "export all translations to cloud storage"
    required: false
  sheetId:
    description: "google sheets id"
    required: false
  addValue:
    description: "New row to add, format category.keyInCamelCase: Translation in English"
    required: false
  uploadRepo:
    description: "The repo to upload to, including repo owner and without the .git extension, not case-sensitive"
    required: false
  uploadPath:
    description: "The repo to upload to, including repo owner and without the .git extension, not case-sensitive"
    required: false
runs:
  using: docker
  image: Dockerfile
  args:
  - "-export=${{ inputs.export }}"
  - "-upload=${{ inputs.upload }}"
  - "-sheetId=${{ inputs.sheetId }}"
  - "-addValue=${{ inputs.addValue }}"
  - "-uploadRepo=${{ inputs.uploadRepo }}"
  - "-uploadPath=${{ inputs.uploadPath }}"
